spring:
  application:
    name: ingestion-service

  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/pulsestack
    username: pulsestack
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    url: jdbc:postgresql://127.0.0.1:5432/pulsestack
    user: pulsestack
    password: password

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

  kafka:
    bootstrap-servers: ${KAFKA_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

pulsestack:
  ingestion:
    poll-interval-hours: 5          # Alle 5h alle Channels pollen
    max-channels: 25
    dedup-ttl-hours: 24             # Redis TTL für gesehene IDs
    rich-preview-limit: 3           # Nur Top-3 bekommen Thumbnail-Fetch

logging:
  level:
    dev.pulsestack: DEBUG
    org.springframework.kafka: INFO
  pattern:
    console: "%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
